<h1> <%= @user.username %>'s Profile </h1>
<%= @user.description %> <br>
<%= @user.location %>

<% if @user == current_user %>
<h2> Make a new post</h2>

<%= form_with(model: @post, local: true) do |f| %>
<%= hidden_field_tag :user_id, @user.id %>
<%= f.hidden_field :post_id %>

  <%= f.label :title %><br>
  <%= f.text_field :title%> <br>

  <%= f.label :body %> <br>
  <%= f.text_area :body %> <br>

  <%= f.submit %><br>
  <% end %>
  <% end %>
<br>
<hr>
<hr>


<h3>Most recent posts:</h3>

  <% @user.posts.reverse.each do |post| %>
  <%= post.title %><br>
  <%= post.body %><br>
  Post by <%= link_to "#{@user.username}", "/#{@user.id}" %>
<br>
  <%= link_to "Comment", "/posts/#{@post_id}" %>
  <%= form_with(model: @comment, local: true) do |f| %>
    <%= hidden_field_tag :user_id, @user.id %>
    <%= f.hidden_field :comment_id %>
    <%= f.hidden_field :post_id %>

    <%= f.text_field :body %>
    <%= f.submit :Comment %>
    <% end %>
  <hr>
  <% end %>

  <%= @user.comments.each do |comment| %>
  <%= comment.body %>
  <% end %>




  <%= form_with(model: @comment, local: true) do |f| %>
    <%= hidden_field_tag :user_id, @user.id %>
    <%= f.hidden_field :post_id %>
    <%= f.text_field :body %>
    <%= f.submit :Comment %>
    <% end %>








<%= link_to 'Edit your account', edit_user_path(@user) %>
<%= link_to 'Delete account', @user, method: 'delete', data: { confirm: 'Are you sure you want to delete your account?' }, local: true  %>
